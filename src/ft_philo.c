#include "../include/philosophers.h"

void    ft_eat(t_philo *ph)
{
    printf("%d philo %d is thinking\n", get_timestamp(), t);
    usleep(1000 * &ph.str_pa->nb_p);   
}

void    ft_sleep(t_philo *ph)
{
    printf("%d philo %d is thinking\n", get_timestamp(), t);
    usleep(1000 * lst->t_sleep);
}

void    ft_think(t_philo *ph)
{
    printf("%d philo %d is thinking\n", get_timestamp(), t);
}   